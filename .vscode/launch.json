{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Edge (Open index.html)",
            "type": "msedge",
            "request": "launch",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}
